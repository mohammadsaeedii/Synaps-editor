# Optional: override the Nest rewrite target (default http://localhost:3001)
# API_PROXY_ORIGIN=http://localhost:3001
# Optional: absolute API URL if not using Next rewrites
# NEXT_PUBLIC_API_URL=/api
